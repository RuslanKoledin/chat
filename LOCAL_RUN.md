# Запуск проекта локально на Mac

## ⚠️ Проблема с Java версией

Проект требует **Java 17**, но у вас установлена **Java 24/25**.
Lombok (библиотека для генерации кода) не работает стабильно с Java 24+.

### Решение: Установить Java 17

```bash
# Установить Java 17 через Homebrew
brew install openjdk@17

# Сделать Java 17 активной
export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"

# Проверить версию
java --version  # Должно показать Java 17
```

Добавьте в `~/.zshrc` (или `~/.bash_profile`):
```bash
export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"
```

---

## 📋 Полная инструкция по запуску

### 1. PostgreSQL ✅ (уже настроено)

PostgreSQL уже запущен и настроен:
- База данных: `messenger_db`
- Пользователь: `messenger_user`
- Пароль: `messenger_password`

Если нужно перезапустить:
```bash
brew services restart postgresql@14
```

### 2. Запустить Backend

После установки Java 17:

```bash
cd messenger-backend
mvn clean spring-boot:run
```

Дождитесь сообщения:
```
Started MessengerApplication in X seconds
```

Backend будет доступен на: `http://localhost:8080`

### 3. Запустить Frontend

В новом терминале:

```bash
cd messenger-frontend
npm start
```

Browser автоматически откроется на: `http://localhost:3000`

### 4. Создать тестовых пользователей

После запуска backend:

```bash
./create-test-users.sh
```

Или вручную:
```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "ivan.petrov",
    "password": "password123",
    "fullName": "Иван Петров",
    "email": "ivan.petrov@company.com",
    "department": "IT"
  }'
```

---

## 🧪 Тестирование

1. Откройте `http://localhost:3000`
2. Войдите: `ivan.petrov` / `password123`
3. Нажмите "+ Новый чат"
4. Создайте чат с другими пользователями
5. Отправьте сообщение

**Real-time тест:**
- Откройте второе окно браузера (Incognito)
- Войдите как `maria.ivanova` / `password123`
- Создайте или откройте общий чат
- Отправляйте сообщения из обоих окон - они появятся мгновенно!

---

## 🔍 Проверка работы

### Backend health check
```bash
curl http://localhost:8080/api/users
```

Должен вернуть `401 Unauthorized` (это нормально - требуется авторизация)

### Frontend
Откройте `http://localhost:3000` - должна появиться страница входа

### PostgreSQL
```bash
psql -h localhost -U messenger_user -d messenger_db
```

---

## 🛑 Остановка сервисов

```bash
# Остановить Backend: Ctrl+C в терминале backend
# Остановить Frontend: Ctrl+C в терминале frontend

# Или убить процессы по портам:
lsof -ti:8080 | xargs kill -9  # Backend
lsof -ti:3000 | xargs kill -9  # Frontend
```

---

## 🐛 Troubleshooting

### Ошибка компиляции Lombok

**Проблема:** `cannot find symbol: method getId()`

**Причина:** Несовместимая версия Java (24/25 вместо 17)

**Решение:** Установите Java 17 (см. выше)

### PostgreSQL не запускается

```bash
brew services restart postgresql@14
brew services list
```

### Порт занят

```bash
# Проверить что занимает порт
lsof -i :8080
lsof -i :3000

# Убить процесс
kill -9 <PID>
```

### Frontend не собирается

```bash
cd messenger-frontend
rm -rf node_modules package-lock.json
npm install
npm start
```

---

## 📝 Полезные команды

```bash
# Проверить Java версию
java --version

# Проверить Maven
mvn --version

# Проверить Node.js
node --version

# Проверить PostgreSQL
psql postgres -c "SELECT version();"

# Просмотр логов backend (если запущен в фоне)
tail -f messenger-backend/logs/spring.log

# Проверить таблицы в БД
psql -U messenger_user -d messenger_db -c "\dt"
```

---

## ✅ Когда всё работает

Вы должны увидеть:

1. ✅ Backend: `Started MessengerApplication`
2. ✅ Frontend: `Compiled successfully!` + браузер открылся
3. ✅ PostgreSQL: `psql` подключается успешно
4. ✅ Страница входа загружается в браузере
5. ✅ После входа видно интерфейс мессенджера

---

## 🎯 Следующие шаги

После успешного запуска локально:

1. Протестируйте все функции
2. Создайте несколько чатов
3. Отправьте сообщения
4. Проверьте real-time обновления
5. Протестируйте ответы на сообщения (reply)

Когда будете готовы деплоить на FUDO:
- Смотрите `DEPLOY_FUDO.md` - полная инструкция
- Смотрите `FUDO_CHEATSHEET.md` - быстрая шпаргалка
