# Корпоративный мессенджер

Полнофункциональная система обмена сообщениями для внутренней коммуникации сотрудников компании.

## Описание проекта

Это MVL (минимально жизнеспособный уровень) реализация корпоративного мессенджера согласно техническому заданию.

### Реализованный функционал

- ✅ Авторизация и аутентификация (JWT)
- ✅ Личные чаты (1:1)
- ✅ Групповые чаты
- ✅ Отправка и получение текстовых сообщений
- ✅ Обмен сообщениями в реальном времени (WebSocket)
- ✅ История переписки
- ✅ Ответы на сообщения (reply)
- ✅ Список доступных пользователей
- ✅ Веб-интерфейс (SPA)

### Технологии

**Backend:**
- Java 17
- Spring Boot 3.2.1
- Spring Security + JWT
- Spring Data JPA
- PostgreSQL 15
- WebSocket (STOMP)
- Maven

**Frontend:**
- React 18 (TypeScript)
- React Router
- Axios
- STOMP.js / SockJS
- CSS3

**Инфраструктура:**
- Docker (для PostgreSQL)
- Nginx (для проксирования)

## Структура проекта

```
chat/
├── messenger-backend/     # Spring Boot backend
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/company/messenger/
│   │   │   │   ├── config/        # Spring конфигурация
│   │   │   │   ├── controller/    # REST контроллеры
│   │   │   │   ├── dto/           # Data Transfer Objects
│   │   │   │   ├── entity/        # JPA сущности
│   │   │   │   ├── repository/    # Spring Data repositories
│   │   │   │   ├── security/      # JWT и Security
│   │   │   │   ├── service/       # Бизнес-логика
│   │   │   │   └── websocket/     # WebSocket handlers
│   │   │   └── resources/
│   │   └── test/
│   ├── pom.xml
│   └── docker-compose.yml
├── messenger-frontend/    # React frontend
│   ├── src/
│   │   ├── api/          # API клиенты
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы
│   │   ├── services/     # WebSocket сервис
│   │   └── types/        # TypeScript типы
│   ├── package.json
│   └── .env
├── nginx.conf            # Nginx конфигурация
└── заданиеТЗ.txt         # Техническое задание
```

## Быстрый старт

### Предварительные требования

- Java 17+
- Node.js 16+
- Docker и Docker Compose
- Maven

### 1. Запустить PostgreSQL

**Вариант А: С Docker (рекомендуется для разработки)**

```bash
cd messenger-backend
docker-compose up -d
```

**Вариант Б: Без Docker (для серверов без Docker)**

Если Docker недоступен (например, на сервере FUDO), установите PostgreSQL напрямую:

📖 **Смотрите подробные инструкции в файле: `INSTALL_POSTGRES_NO_DOCKER.md`**

Краткая версия для Linux:
```bash
# Установить PostgreSQL
sudo yum install -y postgresql15-server postgresql15
sudo /usr/pgsql-15/bin/postgresql-15-setup initdb
sudo systemctl start postgresql-15

# Создать БД и пользователя
sudo -u postgres psql
CREATE DATABASE messenger_db;
CREATE USER messenger_user WITH PASSWORD 'messenger_password';
GRANT ALL PRIVILEGES ON DATABASE messenger_db TO messenger_user;
\c messenger_db
GRANT ALL ON SCHEMA public TO messenger_user;
\q
```

База данных будет доступна на `localhost:5432`

### 2. Запустить Backend

```bash
cd messenger-backend
mvn clean install
mvn spring-boot:run
```

Backend будет доступен на `http://localhost:8080`

### 3. Запустить Frontend

```bash
cd messenger-frontend
npm install
npm start
```

Frontend будет доступен на `http://localhost:3000`

### 4. Создать тестовых пользователей

Выполните POST запросы для регистрации пользователей:

```bash
# Пользователь 1
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "ivan.petrov",
    "password": "password123",
    "fullName": "Иван Петров",
    "email": "ivan.petrov@company.com",
    "department": "IT"
  }'

# Пользователь 2
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "maria.ivanova",
    "password": "password123",
    "fullName": "Мария Иванова",
    "email": "maria.ivanova@company.com",
    "department": "HR"
  }'

# Пользователь 3
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "alex.sidorov",
    "password": "password123",
    "fullName": "Александр Сидоров",
    "email": "alex.sidorov@company.com",
    "department": "Sales"
  }'
```

### 5. Использование приложения

1. Откройте `http://localhost:3000` в браузере
2. Войдите используя один из созданных аккаунтов (например, `ivan.petrov` / `password123`)
3. Создайте новый чат, выбрав пользователей
4. Начните обмен сообщениями!

## API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Пользователи
- `GET /api/users` - Список всех пользователей
- `GET /api/users/{id}` - Получить пользователя по ID

### Чаты
- `GET /api/chats` - Список чатов текущего пользователя
- `GET /api/chats/{id}` - Получить чат по ID
- `POST /api/chats` - Создать новый чат

### Сообщения
- `GET /api/messages/chat/{chatId}` - Получить сообщения чата
- `POST /api/messages` - Отправить сообщение

### WebSocket
- Endpoint: `/ws`
- Subscribe: `/topic/chat/{chatId}`
- Send: `/app/chat.sendMessage`

## База данных

### Схема БД

**users**
- id, username, password, full_name, email, department, active, role, created_at, updated_at

**chats**
- id, name, type (DIRECT/GROUP), created_at

**chat_members** (связь многие-ко-многим)
- chat_id, user_id

**messages**
- id, content, chat_id, sender_id, reply_to_id, created_at

## Конфигурация

### Backend (application.properties)
- Порт сервера: 8080
- База данных: PostgreSQL (localhost:5432/messenger_db)
- JWT secret: настраивается в production
- JWT expiration: 24 часа

### Frontend (.env)
```
REACT_APP_API_URL=http://localhost:8080/api
REACT_APP_WS_URL=http://localhost:8080/ws
```

## Развертывание с Nginx

1. Установите Nginx
2. Скопируйте `nginx.conf` в конфигурацию Nginx
3. Запустите все сервисы
4. Приложение будет доступно на `http://localhost`

## Безопасность

- Все API endpoints (кроме `/api/auth/**`) защищены JWT аутентификацией
- Пароли хранятся в хешированном виде (BCrypt)
- CORS настроен для разрешенных источников
- В production необходимо:
  - Изменить JWT secret на криптостойкий ключ
  - Настроить HTTPS
  - Настроить CORS для реальных доменов
  - Включить rate limiting

## Что НЕ входит в MVL

- Обмен файлами
- Поиск по истории сообщений
- Push-уведомления
- Мобильные приложения
- Внешние интеграции
- Интеграция с AD/LDAP

## Дальнейшее развитие

Возможные улучшения:
- Интеграция с корпоративным AD/LDAP
- Обмен файлами и изображениями
- Полнотекстовый поиск по сообщениям
- Push-уведомления
- Мобильный клиент
- Видео/аудио звонки
- Статусы пользователей (онлайн/офлайн)
- Типизация сообщений (typing indicator)
- Редактирование и удаление сообщений
- Реакции на сообщения

## Лицензия

Проект создан для внутреннего использования компании.
